package Day3.Recursion;

public class First {
    public static void main(String[] args){
        Map<Character, int> myDict = new HashMap<>();
        String str1="ABC";
        String str2="CBA";
        if(str1.length!=str1.length()){break;}
        for(int i=0;i<str.length();i++){
            myDict.put(str1.charAt(i), 1);
        }
        for(int i=0;i<str2.length();i++){
            if(get(str2.charAt(i))){
                myDict.remove(str2.charAt(i))
            }
        }
        print(myDict)
    }
}
